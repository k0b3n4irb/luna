# luna CLI / API reference — moved

This page has moved to the mdBook guide, which is the single maintained
reference for the CLI, the `luna-api` Rust surface, and the MCP tool
catalogue:

- **Source:** [`book/src/using/cli-api-mcp.md`](../book/src/using/cli-api-mcp.md)
- **Published:** <https://k0b3n4irb.github.io/luna/using/cli-api-mcp.html>

> **Source of truth.** The CLI is self-documenting via `clap`: run
> `luna <command> --help` for the canonical, always-current flag list.
> The Rust API is documented inline — `cargo doc -p luna-api --open`.

(The previous copy of this document had drifted from the book — two
curated overviews of the same surface guarantee divergence, so only the
book version is kept.)
